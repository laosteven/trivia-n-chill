services:
  jeopardy:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - CONFIG_PATH=/config/game.yaml
    volumes:
      - ./config:/config:ro
    restart: unless-stopped
