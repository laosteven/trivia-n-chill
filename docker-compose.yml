services:
  trivia-n-chill:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - GAME_TITLE=${GAME_TITLE:-Trivia & Chill}
    volumes:
      - ./config:/app/config:ro
    restart: unless-stopped
