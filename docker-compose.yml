services:
  jeopardy:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - CONFIG_PATH=/config/game.yaml
      - GAME_TITLE=${GAME_TITLE:-Jeopardy!}
    volumes:
      - ./config:/config:ro
    restart: unless-stopped
